#lang sicp
;yes she is correct, by reducing the number of uncertain variables we can
;make our program compute the formula using just the known variables,
;then it will be more accurate, as there is no intermediate approximation
;steps which would result in higher uncertainty.