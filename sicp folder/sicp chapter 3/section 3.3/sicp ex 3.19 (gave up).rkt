#lang sicp

;they said it requires a very clever trick, so yea i gave up.
;its the cycle detection floyds algorithm (used in interviews etc), so
;yea find out more about it regarding the other solutions.