#lang sicp

;not too sure but:
;we need to initialise the procedures by running it, so that it propagates
;over the whole systems to induce changes, if not the whole wires will
;all be at zero, and then when u add new things inside it, it wont really
;propagate correctly.