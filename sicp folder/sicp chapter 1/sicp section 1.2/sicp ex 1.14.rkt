#lang sicp
;space complexity is O(n) as it is the depth of the tree, which
;increases linearly with amount. Time complexity is O(n^5), as when
;you increase n by 1, u will branch out another 5 times.
;(branching factor?). its like there will be 5 other instances of
;the original one that is not increased.
